
@model Nullable<DateTime>

@{
    DateTime dt = DateTime.Now;
    if (Model != null)
    {
        dt = (System.DateTime)Model;

    }
    @Html.TextBox("", String.Format("{0:yyyy/MM/dd }", dt),
     new { @class = "datefield form-control", type = "string", @placeholder = "YYYY/MM/DD" })
} 